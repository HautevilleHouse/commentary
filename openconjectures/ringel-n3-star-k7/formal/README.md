# Lean-native replay

This checks the seven listed `K1,3` stars partition all 21 edges of `K7`.
The result is bounded to this `n=3` slice; general Ringel remains open.

Run `lean formal/RingelN3Star.lean` and `python3 verify.py` from the packet root.
