# Pinned Mathlib replay

This wrapper rechecks the packet's two polynomial nonnegativity certificates
under its pinned Mathlib dependency. Run from `lean/` with `lake env lean
../formal/mathlib/NineArithmeticFunctionInequalitiesMathlib.lean`.
