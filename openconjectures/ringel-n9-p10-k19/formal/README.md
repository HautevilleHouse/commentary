# Lean-native replay

This checks the base `P10` embedding and all 19 cyclic shifts partition all
171 edges of `K19`. The result is bounded to this `n=9` slice; general Ringel
remains open.

Run `lean formal/RingelN9.lean` and `python3 verify.py` from the packet root.
