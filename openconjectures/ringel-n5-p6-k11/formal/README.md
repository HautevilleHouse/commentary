# Lean-native replay

This checks the eleven listed `P6` paths partition all 55 edges of `K11`.
The result is bounded to this `n=5` slice; general Ringel remains open.

Run `lean formal/RingelN5.lean` and `python3 verify.py` from the packet root.
