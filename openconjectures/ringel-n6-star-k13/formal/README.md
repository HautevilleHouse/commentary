# Lean-native replay

This checks the thirteen listed `K1,6` stars partition all 78 edges of `K13`.
The result is bounded to this `n=6` slice; general Ringel remains open.

Run `lean formal/RingelN6Star.lean` and `python3 verify.py` from the packet root.
