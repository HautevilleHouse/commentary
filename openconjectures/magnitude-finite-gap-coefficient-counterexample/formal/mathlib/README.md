# Pinned Mathlib replay

This wrapper rechecks the determinant-limit obstruction using the packet's
pinned Mathlib dependency. Run from `lean/` with `lake env lean
../formal/mathlib/MagnitudeDisproofMathlib.lean`.
