# Lean-native replay

This checks the nine listed `P5` paths partition all 36 edges of `K9`.
The proof is bounded to this `n=4` slice; general Ringel remains open.

Run `lean formal/RingelN4.lean` and `python3 verify.py` from the packet root.
