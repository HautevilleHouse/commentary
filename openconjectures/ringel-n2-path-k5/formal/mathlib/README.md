# Mathlib-backed replay

This file repeats the finite five-path partition of `K5` in a separately
pinned Mathlib environment. It proves list distinctness, path validity, and
the two-way finite edge-membership check.

Verified with Mathlib `17b4b96c4eb874624e9cab005e966a25fd68ab14` and
`leanprover/lean4:v4.33.0-rc1` using `lake env lean`.
