# Lean-native replay

This checks the fifteen listed `P8` paths partition all 105 edges of `K15`.
The result is bounded to this `n=7` slice; general Ringel remains open.

Run `lean formal/RingelN7.lean` and `python3 verify.py` from the packet root.
