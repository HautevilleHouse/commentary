# Lean-native replay

This checks the source-defined 21 modular `K1,10` stars partition all 210
edges of `K21`. The result is bounded to `n=10`; general Ringel remains open.

Run `lean formal/RingelN10Star.lean` and `python3 verify.py` from the packet root.
