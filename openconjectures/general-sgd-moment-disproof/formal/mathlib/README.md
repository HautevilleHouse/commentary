# Pinned Mathlib replay

This wrapper rechecks the packet's finite-third/infinite-sixth moment boundary
under its pinned Mathlib dependency (`v4.28.0`). From the packet's `lean/`
directory, run:

`lake env lean ../formal/mathlib/GeneralSGDMomentDisproofMathlib.lean`
