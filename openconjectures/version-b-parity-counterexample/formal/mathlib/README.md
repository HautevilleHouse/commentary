# Pinned Mathlib replay

This wrapper rechecks the exact finite witness and all-piles move bridge under
the packet's pinned Mathlib dependency. Run from `lean/` with `lake env lean
../formal/mathlib/VersionBParityCounterexampleMathlib.lean`.
