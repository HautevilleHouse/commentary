# Pinned Mathlib replay

This wrapper rechecks both explicit `K₇` edge partitions under the pinned
Mathlib environment. Run from the repository's Lean environment with:

`lake env lean formal/mathlib/RingelN3TwoTreesMathlib.lean`
