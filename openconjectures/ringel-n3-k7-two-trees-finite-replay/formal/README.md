# Lean finite certificate

The explicit cyclic path and star families are checked to equal all 21 edges
of `K₇`. This is only the bounded `n=3` slice.

Run with `lake env lean formal/RingelN3TwoTrees.lean` in a pinned Lean/Mathlib
environment.
