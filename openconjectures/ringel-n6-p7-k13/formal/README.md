# Lean-native replay

This checks the thirteen listed `P7` paths partition all 78 edges of `K13`.
The result is bounded to this `n=6` slice; general Ringel remains open.

Run `lean formal/RingelN6.lean` and `python3 verify.py` from the packet root.
