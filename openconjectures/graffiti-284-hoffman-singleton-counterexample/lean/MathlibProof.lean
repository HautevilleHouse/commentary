import Graffiti284HoffmanSingletonCounterexample

namespace Graffiti284HoffmanSingletonCounterexample

/-! Explicit Lake companion file for the published packet. -/

theorem mathlibProof_imports_core : True := by
  have _ := @hs_all_ones_distance_eigenvalue
  have _ := @hs_distance_from_adj_two
  have _ := @hs_distance_from_adj_neg_three
  have _ := @hs_lambda_min_is_neg_four
  have _ := @hoffman_singleton_refutes_graffiti284_numerics
  have _ := @hoffman_singleton_counterexample_margin
  have _ := @petersen_distance_from_adjacency
  trivial

end Graffiti284HoffmanSingletonCounterexample
