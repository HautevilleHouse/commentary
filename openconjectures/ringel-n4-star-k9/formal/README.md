# Lean-native replay

This checks the nine listed `K1,4` stars partition all 36 edges of `K9`.
The result is bounded to this `n=4` slice; general Ringel remains open.

Run `lean formal/RingelN4Star.lean` and `python3 verify.py` from the packet root.
