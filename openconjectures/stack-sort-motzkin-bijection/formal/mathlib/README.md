# Pinned Mathlib replay

This wrapper rechecks the source-bound inverse-row-length existence theorem
under the packet's pinned Mathlib dependency. Run from `lean/` with `lake env
lean ../formal/mathlib/StackSortMotzkinBijectionMathlib.lean`.
