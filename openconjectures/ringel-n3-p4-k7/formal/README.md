# Lean-native replay

This file checks the exact seven listed `P4` paths partition the 21 edges of
`K7`: path validity, edge-list distinctness, and the two-way finite membership
check. The general Ringel conjecture remains open.

Run `lean formal/RingelN3.lean` and `python3 verify.py` from the packet root.
