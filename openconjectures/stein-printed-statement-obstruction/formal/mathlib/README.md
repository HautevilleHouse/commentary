# Pinned Mathlib replay

This wrapper rechecks the absolute-value counterexample under the packet's
pinned Mathlib dependency. Run from `lean/` with `lake env lean
../formal/mathlib/SteinDisproofMathlib.lean`.
