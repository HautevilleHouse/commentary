# Pinned Mathlib replay

This wrapper rechecks the exact n=7 cycle and formula mismatch under the
packet's pinned Mathlib dependency. Run from `lean/` with `lake env lean
../formal/mathlib/SLCGMaximumGeneratorCounterexampleMathlib.lean`.
